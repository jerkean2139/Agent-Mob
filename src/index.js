import './index.css';
import React from "react";
import ReactDOM from "react-dom";
import App from "./App";

// Ensure React renders the App into the root div in public/index.html
ReactDOM.render(<App />, document.getElementById("root"));
